{
  "name": "wois-assistant-suite",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm --prefix frontend install && npm --prefix electron_app install",
    "dev:backend": "python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload",
    "dev:frontend": "npm --prefix frontend start",
    "dev:electron": "npm --prefix electron_app run dev",
    "dev": "concurrently -k \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:electron\"",
    "build:frontend": "npm --prefix frontend run build",
    "ship:mac": "npm run build:frontend && npm --prefix electron_app run dist:mac"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "wait-on": "^7.2.0"
  }
}